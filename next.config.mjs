/** @type {import('next').NextConfig} */
const nextConfig = {
  // ⚠️(2026-09 성능 최적화): 개발 서버(next dev)는 기본적으로 방금 안 본 페이지를
  // 금방 메모리에서 내려버립니다(기본값: 25초 지나거나, 다른 페이지를 2개 넘게
  // 옮겨다니면 폐기). 이 프로젝트는 페이지가 많아서(관리자 5개 + 사이트 20개 이상)
  // "답변보기 → 관리자 → 답변보기"처럼 왔다갔다만 해도 매번 처음부터 다시
  // 컴파일하고 있었습니다. 한 번 열어본 페이지는 개발 세션 내내 메모리에 남겨두도록
  // 넉넉하게 늘려서, 두 번째 방문부터는 즉시 뜨게 했습니다. (프로덕션 빌드에는
  // 영향 없음 — 개발 서버 전용 옵션입니다.)
  onDemandEntries: {
    maxInactiveAge: 60 * 60 * 1000, // 1시간 동안은 안 내림
    pagesBufferLength: 15, // 이 프로젝트의 주요 페이지 수만큼 넉넉하게
  },
};
export default nextConfig;
